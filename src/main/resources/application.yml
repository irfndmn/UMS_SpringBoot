server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/springboot_db_ums
    username: dev_user
    password: password

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect






